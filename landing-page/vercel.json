{
  "framework": "nextjs",
  "buildCommand": "npm test && next build",
  "installCommand": "npm install",
  "crons": [
    {
      "path": "/api/cron/uptime",
      "schedule": "*/5 * * * *"
    }
  ]
}
